DEBUG=true
SECRET_KEY=replace-with-a-long-random-value
ALLOWED_HOSTS=localhost,127.0.0.1
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
TIME_ZONE=Asia/Kolkata
# POSTGRES_DB=lenspirecrm
# POSTGRES_USER=lenspirecrm
# POSTGRES_PASSWORD=change-me
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432
BACKUP_ENCRYPTION_KEY=replace-with-a-dedicated-random-backup-key
CELERY_BROKER_URL=redis://localhost:6379/0
# Optional: two-way CRM calendar mirror. Sheet changes are checked every five
# minutes. Share the target sheet with the service-account email as an Editor,
# then set its key JSON as a single base64 value.
# GOOGLE_SHEETS_SYNC_SPREADSHEET_ID=1Wm3o99vBTuuGiTc8TPhH51T0vKET8mwgskeXxvKdxLk
# GOOGLE_SHEETS_SERVICE_ACCOUNT_JSON_B64=base64-encoded-service-account-json
# Allows multipart form-data overhead; quotation files remain limited to 25 MB.
# REQUEST_BODY_MAX_SIZE=26738688
